import Vue from 'vue'
import Vuex from 'vuex'
// import historyGraphStore from "@/components/History/store";

Vue.use(Vuex)

export default new Vuex.Store({
    // modules: { historyGraphStore }
    state: {}
});
